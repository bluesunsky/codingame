object Player extends App{
    while(true) {
        val e = readLine
        val d = readInt
        val E = readLine
        val D = readInt
        if (d<D) {println(e)}
        else{println(E)}
    }
}